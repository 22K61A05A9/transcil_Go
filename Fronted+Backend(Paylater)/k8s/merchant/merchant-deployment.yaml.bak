apiVersion: apps/v1
kind: Deployment

metadata:
  name: merchant
  namespace: paylater

spec:
  replicas: 1

  selector:
    matchLabels:
      app: merchant

  template:
    metadata:
      labels:
        app: merchant

    spec:
      containers:
        - name: merchant
          image: frontedbackendpaylater-merchant
          imagePullPolicy: IfNotPresent

          ports:
            - containerPort: 9094
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
          env:
            - name: DB_HOST
              value: mysql

            - name: DB_PORT
              value: "3306"

            - name: DB_NAME
              value: paylater

            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: merchant-secret
                  key: DB_USER

            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: merchant-secret
                  key: DB_PASSWORD

            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: merchant-secret
                  key: JWT_SECRET

            - name: INTERNAL_SERVICE_TOKEN
              valueFrom:
                secretKeyRef:
                  name: merchant-secret
                  key: INTERNAL_SERVICE_TOKEN

          readinessProbe:
            httpGet:
              path: /health
              port: 9094
            initialDelaySeconds: 10
            periodSeconds: 5

          livenessProbe:
            httpGet:
              path: /health
              port: 9094
            initialDelaySeconds: 30
            periodSeconds: 10
