# Public Vite env (exposed to the browser). Never put secrets here.
# INTERNAL_SERVICE_TOKEN must NEVER appear in frontend env files.

# API base URL used by the shared HTTP client (when added).
# Local development: use the Vite proxy prefix so the browser stays same-origin.
VITE_API_BASE_URL=/api
