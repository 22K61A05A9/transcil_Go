apiVersion: v1
kind: Service

metadata:
  name: transaction
  namespace: paylater

spec:
  selector:
    app: transaction

  ports:
    - protocol: TCP
      port: 9092
      targetPort: 9092

  type: ClusterIP
